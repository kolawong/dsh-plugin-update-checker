# DeepSeek Harness — System & Plugin Manager Bundle Patch
- insert:
    - id: update-checker
      name: 'dsh-plugin-update-checker'
      inject:
        - webServer
      config:
        autoCheck: true
        checkIntervalMinutes: 30
        githubRepo: 'deepseek-ai/deepseek-harness'
